# Cardio Disease Prediction API (Flask + ML)

This project predicts the risk of cardiovascular (heart) disease using a trained Machine Learning model on the **Cardio Dataset**.  
It also provides a **Flask REST API** so you can send patient details and get predictions.

---

## 📌 Features
- Trained ML model for heart disease prediction  
- Flask API for real-time predictions  
- Scalers (`x_scaler`, `y_scaler`) used to preprocess inputs  
- Easy to test using Postman / cURL / frontend
